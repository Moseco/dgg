name: dgg
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.11.1+21

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # State management
  stacked: ^2.2.7+1
  stacked_services: ^0.8.16
  stacked_themes: ^0.3.8+1
  stacked_hooks: ^0.2.1+1

  # Firebase
  firebase_core: ^1.0.3
  firebase_crashlytics: ^2.0.0
  firebase_analytics: ^8.0.0

  # UI
  flutter_staggered_animations: ^1.0.0
  flutter_settings_ui: ^2.0.1
  adaptive_dialog: ^0.10.0+5
  percent_indicator: ^3.0.1
  extended_nested_scroll_view: ^3.0.0
  introduction_screen: ^2.1.0

  # Other
  shared_preferences: ^2.0.5
  http: ^0.13.1
  webview_flutter: ^3.0.0
  webview_cookie_manager: ^2.0.0
  web_socket_channel: ^2.0.0
  url_launcher: ^6.0.3
  flutter_custom_tabs: ^1.0.3
  image: ^3.0.2
  wakelock: ^0.5.6
  youtube_player_iframe: ^2.0.0
  flutter_cache_manager: ^3.1.1
  intl: ^0.17.0
  flutter_hooks: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  stacked_generator: ^0.5.6

  flutter_launcher_icons: ^0.9.0

  flutter_lints: ^1.0.4

#TODO need to make iOS icons later
#To run:
#flutter pub run flutter_launcher_icons:main
flutter_icons:
  android: false
  ios: false
  image_path_android: "assets/images/dgg_icon.png"
  adaptive_icon_background: "#1A1A1A"
  adaptive_icon_foreground: "assets/images/dgg_icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/dgg_icon.png
    - assets/images/dgg_logo.png
    - assets/images/login_key.png
    - assets/images/gameofthrows.png
    - assets/images/godstiny.png
